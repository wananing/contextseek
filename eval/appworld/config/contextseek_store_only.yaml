experiment_name: appworld_contextseek_store_only
dataset: test_normal
max_tasks: 0
resume: true
output_dir: ./output/appworld

appworld:
  python: ${APPWORLD_PYTHON}

agent:
  type: contextseek_react
  model: gpt-4o
  max_steps: 25
  temperature: 0.0
  llm_api_key: ${OPENAI_API_KEY}
  # OpenAI-compatible API base URL (optional):
  llm_base_url: ${LLM_BASE_URL}
  llm_provider: openai
  store_only: true
  auto_compact: false

contextseek:
  enabled: true
  scope: appworld/shared/test_normal/global
  storage:
    backend: file
    path: .contextseek/appworld
  evolution:
    enabled: false
    extract_min_age_seconds: 0
  observability:
    audit_enabled: true
    audit_path: .contextseek/appworld_audit.jsonl

distill:
  compact_after: true
  max_records: null
