# Copy to .env and fill in values before running the demo.

# Required: OpenAI-compatible API key
OPENAI_API_KEY=sk-...

# Optional: custom model name (default: gpt-4o)
# LLM_MODEL=gpt-4o

# Optional: custom base URL for self-hosted or proxy endpoints
# LLM_BASE_URL=https://your-endpoint/v1
